# Compiler Pipeline Output

This folder contains artifacts generated from the file `hello.c`:

- `hello.i` — Preprocessed source  
- `hello.s` — Assembly output  
- `hello.o` — Object file  
- `hello.bin` — Final linked executable  
