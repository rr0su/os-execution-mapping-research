# os-execution-mapping-research
A research project documenting the full execution path of a Linux program: compilation → ELF internals → loader behavior → PLT/GOT → memory mapping → calling conventions → micro RE suite. Includes reproducible experiments and diagrams.
